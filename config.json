import os
import json

config = {
    "token": os.environ.get("BOT_TOKEN"),
    "suggestion_channel_id": int(os.environ.get("SUGGESTION_CHANNEL_ID")),
    "main_channel_id": int(os.environ.get("MAIN_CHANNEL_ID")),
    "staff_channel_id": int(os.environ.get("STAFF_CHANNEL_ID")),
    "announcement_channel_id": int(os.environ.get("ANNOUNCEMENT_CHANNEL_ID")),
    "notification_channel_id": int(os.environ.get("NOTIFICATION_CHANNEL_ID")),
    "suggestion_threshold": int(os.environ.get("SUGGESTION_THRESHOLD", 5)),
    "staff_role_id": int(os.environ.get("STAFF_ROLE_ID")),
    "main_role_id": int(os.environ.get("MAIN_ROLE_ID")),
    "king_role_id": int(os.environ.get("KING_ROLE_ID")),
    "buff_giver_role_id": int(os.environ.get("BUFF_GIVER_ROLE_ID")),
    "scheduler_category_id": int(os.environ.get("SCHEDULER_CATEGORY_ID")),
    "staff_vote_timer_seconds": int(os.environ.get("STAFF_VOTE_TIMER_SECONDS", 86400))
}